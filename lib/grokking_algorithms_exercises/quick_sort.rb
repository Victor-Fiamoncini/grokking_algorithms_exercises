# frozen_string_literal: true

module GrokkingAlgorithmsExercises
  # Quick sort algorithm, still an open exercise.
  # Once written it should run in O(n log n) on average, O(n^2) worst case.
  class QuickSort
    def initialize(items = [])
      @items = items.dup
    end

    # TODO: implement quick sort
    def execute
      []
    end
  end
end
